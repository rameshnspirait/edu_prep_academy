import 'package:flutter/material.dart';

class HelpSupportsView extends StatelessWidget {
  const HelpSupportsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Help & Support')),
      body: Container(),
    );
  }
}
